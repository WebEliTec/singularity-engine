{
  "timestamp": "2026-06-06T10:24:34.065Z",
  "metaData": {
    "appName": "athene",
    "appDescription": "Athene is the control surface for the Singularity Engine, a knowledge-modeling system. The user defines entity classes (the types of things being modeled — e.g. plugin, theme) and gives each class a versioned attribute schema: attributes grouped into Attribute Set Versions that move through a draft → committed lifecycle. The app is a thin front end over a world-model backend (Apollo); from here you browse and manage entity classes, edit the attributes of a class, and create / commit / delete schema versions."
  },
  "config": {
    "lazyLoadNodeResources": false,
    "defaultPaths": {
      "node": {
        "modules": "/",
        "components": "/components",
        "kernelFragments": "/kernel"
      }
    }
  },
  "constants": {
    "apolloBaseUrl": "http://localhost:8001"
  },
  "coreData": {
    "clientStorageSchemaVersion": "1"
  },
  "documentation": {
    "architecture": "# Architecture\n\nAthene is a **Morpheus app**: a tree of nodes rooted at `Root`, each node a\nself-contained unit of UI and behaviour backed by a kernel.\n\n## The node tree\n\n| Node | Role |\n|------|------|\n| `Root` | Pure dispatcher — reads the active-page signal and mounts exactly one page-node sibling. Holds no chrome. |\n| `Home` | The brand-identity surface — the engine logo plus the class list as navigation. The only place the logo appears. |\n| `EntityClassRegistry` | The surface for adding / editing / deleting entity classes themselves. |\n| `EntityClassWorkbench` | The per-class surface, scoped to whichever class the user opened — where attributes and schema versions are managed. |\n\n## The backend boundary\n\nAthene holds **no domain state of its own** — it is a projection of Apollo's\nworld model. Reads and writes go through Apollo at `apolloBaseUrl`\n(`http://localhost:8001`). The A5 cutover (2026-05-29) flipped this from the\nlegacy Laravel content-creation-center to the node + Fastify world-model\nbackend.\n\n## Inputs — Hermes\n\nForm inputs across the app are provided by **Hermes**, a global component\nvocabulary (text, select, comboBox, checkboxes, …) registered at the app level\nso any surface can compose the same inputs without copying them per node.\n",
    "conventions": "# Conventions\n\nA few patterns Athene follows.\n\n## Naming\n\n- **Page-nodes** are nouns describing the surface (`Home`,\n  `EntityClassRegistry`, `EntityClassWorkbench`).\n- Visual primitives use Greek-letter variants (`PanelAlpha`, `ListAlpha`,\n  `PageHeaderAlpha`) rather than purpose-bound names, so they stay reusable\n  across surfaces.\n\n## State\n\n- Domain data lives in **Apollo**, not in node signals. Signals hold only\n  view state — what is selected, what is open.\n- The active page is a single signal on `Root`; `Root` mounts one page-node\n  at a time.\n\n## Backend\n\n- All backend calls target `apolloBaseUrl`. There is no second source of truth\n  in the front end.\n",
    "overview": "# Overview\n\nAthene is the **control surface** for the Singularity Engine — a knowledge-modeling\nsystem. It is a thin front end over a world-model backend (**Apollo**); everything\nyou do here ultimately reads from or writes to Apollo.\n\n## What you model\n\nThe core concept is the **entity class** — a *type of thing* being modeled\n(for example `plugin` or `theme`). Each entity class carries a **versioned\nattribute schema**: its attributes are grouped into **Attribute Set Versions**\nthat move through a `draft → committed` lifecycle.\n\n- **Draft** versions are editable — you add, remove, and reshape attributes.\n- **Committed** versions are frozen — a stable schema the rest of the engine\n  can rely on.\n\n## What you do here\n\nFrom Athene you:\n\n- browse and manage the registry of entity classes,\n- edit the attributes of a class,\n- and create / commit / delete schema versions.\n\n## Where it sits\n\nAthene is built with the **Morpheus** meta-framework and talks to Apollo at\n`http://localhost:8001`. See [Architecture](./architecture.md) for how the\npieces fit together, and [Conventions](./conventions.md) for the patterns the\ncodebase follows.\n"
  },
  "nodeRegistry": {
    "Root": {
      "isRoot": true
    },
    "Home": {},
    "EntityClassRegistry": {},
    "EntityClassWorkbench": {},
    "Hermes": {},
    "Chronos": {},
    "EntityClassExplorer": {}
  },
  "globalModules": {},
  "globalComponents": {
    "SingularityLogoAnimated": {},
    "PanelAlpha": {},
    "ListAlpha": {},
    "PageHeaderAlpha": {},
    "HermesInput": {},
    "HermesSimpleInput": {},
    "HermesSelectInput": {},
    "HermesUnsupportedInput": {}
  },
  "mediaRegistry": {},
  "hooks": {
    "appKernelDidInitialize": "[fn:appKernelDidInitialize]"
  },
  "appEvents": {},
  "agentApi": {
    "goToHome": "[fn:goToHome]",
    "goToEntityClasses": "[fn:goToEntityClasses]",
    "openEntityClass": "[fn:openEntityClass]"
  },
  "agentApiSchemas": {
    "goToHome": {
      "description": "Take the user to the Home screen (the brand surface with entity-class navigation).",
      "returns": {
        "type": "object",
        "description": "The surface now shown."
      }
    },
    "goToEntityClasses": {
      "description": "Show the list of entity classes the user can manage (the Entity Class Registry).",
      "returns": {
        "type": "object",
        "description": "The surface now shown."
      }
    },
    "openEntityClass": {
      "description": "Open the management workbench for a specific entity class.",
      "params": {
        "classId": {
          "type": "string",
          "required": true,
          "description": "The id of the entity class to open."
        }
      },
      "returns": {
        "type": "object",
        "description": "The surface now shown and the class id opened."
      }
    }
  },
  "kernelFragments": [],
  "services": {
    "router": {
      "description": "URL routing and navigation",
      "appTypes": [
        "mainApp",
        "devApp"
      ]
    },
    "utility": {
      "description": "General utility functions",
      "appTypes": [
        "mainApp",
        "devApp"
      ]
    },
    "dom": {
      "description": "DOM manipulation utilities",
      "appTypes": [
        "mainApp",
        "devApp"
      ]
    },
    "media": {
      "description": "Media asset management (sounds, images)",
      "appTypes": [
        "mainApp",
        "devApp"
      ]
    },
    "sfx": {
      "description": "Short low-latency sound effects (Web Audio API)",
      "appTypes": [
        "mainApp",
        "devApp"
      ]
    },
    "log": {
      "description": "Centralized logging service (mutations, info, warn, error)",
      "appTypes": [
        "mainApp"
      ]
    },
    "indexedDBManager": {
      "description": "IndexedDB persistence layer",
      "appTypes": [
        "mainApp"
      ]
    },
    "localStorageManager": {
      "description": "LocalStorage persistence layer",
      "appTypes": [
        "mainApp"
      ]
    },
    "agentTransport": {
      "description": "AI control layer (β4): the transport + tool-use loop that lets an LLM operate the running app via getAgentManifest() + callAgentMethod(). The LLM call is an injected seam (setLlmCall) — direct-to-Claude now, Prometheus later.",
      "appTypes": [
        "mainApp",
        "devApp"
      ]
    }
  }
}